
import { type CharacterStats } from "../api/zkill"
import {type TeamConstellationProps} from "./TeamManager"

export function PlayerSmallCard(Player : CharacterStats) {
    return (
        <div>

        </div>
    );
};

export function LargePlayerCard(Player : CharacterStats) {
    return (
        <div>

        </div>
    );
};


export function MatchupDashboard(Players : TeamConstellationProps) {
    return (
        <div>

        </div>
    );
};